const TIME_HOUR = 1000 * 60 * 60;
const TIME_DAY = TIME_HOUR * 24;
const TIME_WEEK = TIME_DAY * 7;
const TIME_MONTH = TIME_DAY * 30;
const USERS_LAMBDA = 'https://444urs2woj.execute-api.us-east-1.amazonaws.com/default/rent-bot-users';

module.exports = {
	TIME_HOUR,
	TIME_DAY,
	TIME_WEEK,
	TIME_MONTH,
	USERS_LAMBDA,
};
